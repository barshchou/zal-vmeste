# Зал вместе

Мобильный статический сайт с двумя 12-недельными полнотелыми программами тренировок. Работает без сборки; опционально — синхронизация через Supabase.

## Что внутри

- отдельные планы для него и для неё;
- тренировки A/B, фазы на 12 недель и правила прогрессии;
- GIF к упражнениям и подробная техника;
- дневник подходов (локально + опционально облако Supabase);
- таймер отдыха на 60, 90 и 120 секунд;
- отдельное питание с калориями, БЖУ и меню;
- адаптация под телефон и компьютер.

Без Supabase дневник хранится только в `localStorage` на устройстве. С Supabase — общий логин на двоих, данные на телефоне и ПК. **Инструкция:** [`SUPABASE.md`](SUPABASE.md).

## Открыть локально

Можно просто открыть `index.html` двойным щелчком. Для корректной проверки через локальный сервер:

```powershell
cd C:\Users\Slava\zal-vmeste
python -m http.server 8000
```

Затем открыть <http://localhost:8000>.

## Опубликовать на GitHub Pages

1. Создать на GitHub пустой репозиторий, например `zal-vmeste`.
2. В папке проекта выполнить:

```powershell
git add .
git commit -m "Create gym training guides"
git branch -M main
git remote add origin https://github.com/ВАШ-ЛОГИН/zal-vmeste.git
git push -u origin main
```

3. На GitHub открыть **Settings → Pages**.
4. В **Build and deployment** выбрать **Deploy from a branch**.
5. Выбрать ветку `main`, папку `/ (root)` и нажать **Save**.
6. Через несколько минут сайт будет доступен по адресу:

```text
https://ВАШ-ЛОГИН.github.io/zal-vmeste/
```

## Структура

```text
index.html          главная и выбор плана
him.html            тренировки для него
her.html            тренировки для неё
technique.html      техника упражнений
advice.html         питание и восстановление
css/style.css       адаптивное оформление
js/data.js          планы и тексты техники
js/app.js           дневник, таймер, поиск
```

## Важно

Материалы сайта носят образовательный характер и не заменяют медицинскую консультацию. Острая боль, боль в груди, предобморочное состояние или необычная одышка требуют прекращения нагрузки; стойкую боль следует оценить очно у врача.
